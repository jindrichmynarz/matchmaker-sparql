{{!
@param IRI graph
}}

{{> templates/prefixes.mustache}}

ASK
WHERE {
  GRAPH <{{graph}}> {
    ?contract pc:awardedTender ?tender .
    ?tender pc:bidder ?bidder .
    FILTER (isBlank(?contract) || isBlank(?tender) || isBlank(?bidder))
  }
}
